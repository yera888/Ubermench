Grouped into 

#### Bus Topology 

- Has a single cable 
- Has something called a T connector (logically designed)which is also used in ethernets -> [[cdcscb]] at the end of the t there needs to be something that can absorb the current and then on the other side there can be a collision.
#### Star Topology

- shaped like a star with a router in the middle and nodes at the end its hob is to receive and filter 
#### Ring Topology

- There is a one way process shaped like a ring with nodes connected on the outskirts of that ring. If a link is faulty the lines that are connected will fail and block the communication with the other lines.

#### Hybrid Topology 

- Extending local something networks and connecting multiple different kinds of topologies above.


### Some info about Layers for LAN
When you first push the data the signal will be very string and over time it will loosen and a repeater will boost the signal once again so it starts. The cable carries the signal and the design is about the length of the cable. However, there is a limit on how many repeaters you can put. 

