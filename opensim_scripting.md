the [[environment]] for the backend it needs 
```conda activate opensim_scripting```
to activate the environment in git bash .

The frontend doesnt need this because React/bite doesnt need OpenSim or FastAPI installed locally in that terminal. 